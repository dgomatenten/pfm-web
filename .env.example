# Flask configuration
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=change-me

# Database configuration
DATABASE_URL=sqlite:///pfm.db
SQLALCHEMY_ECHO=0

# Security / Auth
SESSION_COOKIE_NAME=pfm_session
JWT_SECRET=change-me-too
TOKEN_EXPIRY_MINUTES=60

# Android sync
ANDROID_API_TOKEN=replace-with-shared-secret

# Background worker (future)
REDIS_URL=redis://localhost:6379/0

# Logging
LOG_LEVEL=INFO
